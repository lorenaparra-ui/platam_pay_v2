# Base de datos y app (ajustar según entorno)
# DATABASE_URL=...
# NODE_ENV=development

# Cifrado en reposo para bank_account en tabla suppliers (AES-256).
# Obligatorio si se usa el módulo de suppliers; mínimo 32 caracteres.
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SUPPLIER_BANK_ACCOUNT_ENCRYPTION_KEY=
