import { CollectionFeeRuleEntity } from './collection-fee-rule.entity';

/** Entidades TypeORM en `collections_schema` (migraciones + `CollectionsDataModule`). */
export const COLLECTIONS_DATA_ENTITIES = [CollectionFeeRuleEntity] as const;
